name: first workflow 

on: 
  push:
    branches:
      - main

jobs:
  example-job:
    runs-on: ubuntu-latest

    steps:
      - name: print a welcome message
        run: echo "welcome to our first Github action workflow"
        
      - name: checkout repositry
        uses: actions/checkout@v4.2.2

      - name: list reposity files 
        run: ls -al
